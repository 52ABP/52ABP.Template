@using LTMCompanyNameFree.YoyoCmsTemplate.Web.ViewModels.Common.Modals
@model LTMCompanyNameFree.YoyoCmsTemplate.Web.ViewModels.Users.EditUserModalViewModel
@{
    Layout = null;
}
@Html.Partial("~/Views/Shared/Modals/_ModalHeader.cshtml", new ModalHeaderViewModel(L("EditUser")))

<div class="modal-body">
    <form name="UserEditForm" role="form" novalidate class="form-validation">
        <input type="hidden" name="Id" value="@Model.User.Id" />
        <ul class="nav nav-tabs card-header-tabs ml-md-auto mt-3 mt-md-0" role="tablist">
            <li role="presentation" class="nav-item"><a href="#edit-user-details" class="nav-link active" data-toggle="tab">@L("UserDetails")</a></li>
            <li role="presentation" class="nav-item"><a href="#edit-user-roles" class="nav-link " data-toggle="tab">@L("UserRoles")</a></li>
        </ul>
        <div class="tab-content">
            <div role="tabpanel" class="tab-pane fade active show" id="edit-user-details">

                <div class="row clearfix" style="margin-top:20px;">
                    <div class="col-sm-12">
                        <div class="form-group form-float">
                            <div class="form-line">
                                <label for="username">@L("UserName")</label>
                                <input id="username" type="text" name="UserName" value="@Model.User.UserName" required maxlength="32" minlength="2" class="validate form-control">

                            </div>
                        </div>
                    </div>
                </div>

                <div class="row clearfix">
                    <div class="col-sm-6">
                        <div class="form-group form-float">
                            <div class="form-line">
                                <label for="name">@L("Name")</label>
                                <input id="name" type="text" name="Name" value="@Model.User.Name" required maxlength="32" class="validate form-control">

                            </div>
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="form-group form-float">
                            <div class="form-line">
                                <label for="surname">@L("Surname")</label>
                                <input id="surname" type="text" name="Surname" value="@Model.User.Surname" required maxlength="32" class="validate form-control">

                            </div>
                        </div>
                    </div>
                </div>

                <div class="row clearfix">
                    <div class="col-sm-12">
                        <div class="form-group form-float">
                            <div class="form-line">
                                <label for="email">@L("EmailAddress")</label>
                                <input id="email" type="email" name="EmailAddress" value="@Model.User.EmailAddress" maxlength="256" class="validate form-control">

                            </div>
                        </div>
                    </div>
                </div>

              
                    
                       
                            <div class="custom-control custom-checkbox mb-4 ">
                                <input id="IsActive" type="checkbox" name="IsActive" value="true" @(Model.User.IsActive ? "checked" : "") class="custom-control-input" />
                                <label for="IsActive" class="d-block custom-control-label">@L("IsActive")</label>
                               </div>
                   
            </div>

            <div role="tabpanel" class="tab-pane animated fadeIn" id="edit-user-roles">
                <div class="row">
                    <div class="col-sm-12 ">
                        @foreach (var role in Model.Roles)
                        {
                            <div class="col-sm-6">
                                <input type="checkbox" name="role" value="@role.NormalizedName" title="@role.Description" class="filled-in" id="@string.Format("role{0}",role.Id)" @(Model.UserIsInRole(role) ? "checked" : "") />
                                <label for="@string.Format("role{0}",role.Id)" title="@role.DisplayName">@role.Name</label>
                            </div>
                        }
                    </div>
                </div>
            </div>

        </div>
    </form>
</div>

@Html.Partial("~/Views/Shared/Modals/_ModalFooterWithSaveAndCancel.cshtml")

<script src="~/view-resources/Views/Users/_EditUserModal.js" asp-append-version="true"></script>
